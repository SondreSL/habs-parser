The Language ABS
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of ABS==

===Literals===
String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.


Integer literals //Integer// are nonempty sequences of digits.





U literals are recognized by the regular expression
`````upper (letter | digit | '_')*`````

L literals are recognized by the regular expression
`````lower (letter | digit | '_')*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in ABS are the following:
  | ``assert`` | ``await`` | ``builtin`` | ``case``
  | ``catch`` | ``class`` | ``data`` | ``def``
  | ``else`` | ``exception`` | ``export`` | ``extends``
  | ``fimport`` | ``finally`` | ``from`` | ``get``
  | ``if`` | ``implements`` | ``import`` | ``in``
  | ``interface`` | ``let`` | ``local`` | ``module``
  | ``new`` | ``null`` | ``println`` | ``pro_give``
  | ``pro_new`` | ``pro_try`` | ``return`` | ``skip``
  | ``suspend`` | ``then`` | ``this`` | ``thisDC``
  | ``throw`` | ``try`` | ``type`` | ``while``

The symbols used in ABS are the following:
  | . | , | < | >
  | _ | ; | * | =
  | | | ( | ) | {
  | } | ? | & | ||
  | && | == | != | <=
  | >= | + | - | /
  | % | ~ | [ | ]
  | => | ! | : | 

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of ABS==
Non-terminals are enclosed between < and >. 
The symbols -> (production),  **|**  (union) 
and **eps** (empty rule) belong to the BNF notation. 
All other symbols are terminals.

  | //Literal// | -> | ``null`` 
  |  |  **|**  | ``this`` 
  |  |  **|**  | //String// 
  |  |  **|**  | //Integer// 
  |  |  **|**  | ``thisDC`` 
  | //QU// | -> | //U// 
  |  |  **|**  | //U// ``.`` //QU// 
  | //QL// | -> | //L// 
  |  |  **|**  | //U// ``.`` //QL// 
  | //QA// | -> | //L// 
  |  |  **|**  | //U// 
  |  |  **|**  | //U// ``.`` //QA// 
  | //[U]// | -> | //U// 
  |  |  **|**  | //U// ``,`` //[U]// 
  | //[QU]// | -> | //QU// 
  |  |  **|**  | //QU// ``,`` //[QU]// 
  | //[QA]// | -> | //QA// 
  |  |  **|**  | //QA// ``,`` //[QA]// 
  | //T// | -> | //QU// 
  |  |  **|**  | //QU// ``<`` //[T]// ``>`` 
  |  |  **|**  | ``_`` 
  | //[T]// | -> | //T// 
  |  |  **|**  | //T// ``,`` //[T]// 
  | //Param// | -> | //T// //L// 
  | //[Param]// | -> | **eps**
  |  |  **|**  | //Param// 
  |  |  **|**  | //Param// ``,`` //[Param]// 
  | //Program// | -> | //[Module]// 
  | //Module// | -> | ``module`` //QU// ``;`` //[Export]// //[Import]// //[AnnDecl]// //MaybeBlock// 
  | //[Module]// | -> | **eps**
  |  |  **|**  | //Module// //[Module]// 
  | //Export// | -> | ``export`` ``*`` 
  |  |  **|**  | ``export`` ``*`` ``from`` //QU// 
  |  |  **|**  | ``export`` //[QA]// 
  |  |  **|**  | ``export`` //[QA]// ``from`` //QU// 
  | //[Export]// | -> | **eps**
  |  |  **|**  | //Export// ``;`` //[Export]// 
  | //Import// | -> | //IsForeign// ``*`` ``from`` //QU// 
  |  |  **|**  | //IsForeign// //[QA]// 
  |  |  **|**  | //IsForeign// //[QA]// ``from`` //QU// 
  | //[Import]// | -> | **eps**
  |  |  **|**  | //Import// ``;`` //[Import]// 
  | //IsForeign// | -> | ``import`` 
  |  |  **|**  | ``fimport`` 
  | //Decl// | -> | ``type`` //U// ``=`` //T// ``;`` 
  |  |  **|**  | ``type`` //U// ``<`` //[U]// ``>`` ``=`` //T// ``;`` 
  |  |  **|**  | ``exception`` //ConstrIdent// ``;`` 
  |  |  **|**  | ``data`` //U// ``=`` //[ConstrIdent]// ``;`` 
  |  |  **|**  | ``data`` //U// ``<`` //[U]// ``>`` ``=`` //[ConstrIdent]// ``;`` 
  |  |  **|**  | ``def`` //T// //L// ``(`` //[Param]// ``)`` ``=`` //FunBody// ``;`` 
  |  |  **|**  | ``def`` //T// //L// ``<`` //[U]// ``>`` ``(`` //[Param]// ``)`` ``=`` //FunBody// ``;`` 
  |  |  **|**  | ``interface`` //U// ``{`` //[MethSignat]// ``}`` 
  |  |  **|**  | ``interface`` //U// ``extends`` //[QU]// ``{`` //[MethSignat]// ``}`` 
  |  |  **|**  | ``class`` //U// ``{`` //[ClassBody]// //MaybeBlock// //[ClassBody]// ``}`` 
  |  |  **|**  | ``class`` //U// ``(`` //[Param]// ``)`` ``{`` //[ClassBody]// //MaybeBlock// //[ClassBody]// ``}`` 
  |  |  **|**  | ``class`` //U// ``implements`` //[QU]// ``{`` //[ClassBody]// //MaybeBlock// //[ClassBody]// ``}`` 
  |  |  **|**  | ``class`` //U// ``(`` //[Param]// ``)`` ``implements`` //[QU]// ``{`` //[ClassBody]// //MaybeBlock// //[ClassBody]// ``}`` 
  | //[ConstrIdent]// | -> | //ConstrIdent// 
  |  |  **|**  | //ConstrIdent// ``|`` //[ConstrIdent]// 
  | //ConstrIdent// | -> | //U// 
  |  |  **|**  | //U// ``(`` //[ConstrType]// ``)`` 
  | //[ConstrType]// | -> | **eps**
  |  |  **|**  | //ConstrType// 
  |  |  **|**  | //ConstrType// ``,`` //[ConstrType]// 
  | //ConstrType// | -> | //T// 
  |  |  **|**  | //T// //L// 
  | //FunBody// | -> | ``builtin`` 
  |  |  **|**  | //PureExp// 
  | //[MethSignat]// | -> | **eps**
  |  |  **|**  | //MethSignat// ``;`` //[MethSignat]// 
  | //MethSignat// | -> | //[Ann]// //T// //L// ``(`` //[Param]// ``)`` 
  | //[ClassBody]// | -> | **eps**
  |  |  **|**  | //ClassBody// //[ClassBody]// 
  | //ClassBody// | -> | //T// //L// ``;`` 
  |  |  **|**  | //T// //L// ``=`` //PureExp// ``;`` 
  |  |  **|**  | //T// //L// ``(`` //[Param]// ``)`` ``{`` //[AnnStm]// ``}`` 
  | //Stm// | -> | //Exp// ``;`` 
  |  |  **|**  | ``skip`` ``;`` 
  |  |  **|**  | ``suspend`` ``;`` 
  |  |  **|**  | ``return`` //Exp// ``;`` 
  |  |  **|**  | ``assert`` //PureExp// ``;`` 
  |  |  **|**  | ``println`` //PureExp// ``;`` 
  |  |  **|**  | ``await`` //AwaitGuard// ``;`` 
  |  |  **|**  | //L// ``=`` //Exp// ``;`` 
  |  |  **|**  | ``this`` ``.`` //L// ``=`` //Exp// ``;`` 
  |  |  **|**  | //T// //L// ``;`` 
  |  |  **|**  | //T// //L// ``=`` //Exp// ``;`` 
  |  |  **|**  | ``while`` ``(`` //PureExp// ``)`` //AnnStm// 
  |  |  **|**  | ``if`` ``(`` //PureExp// ``)`` //Stm// 
  |  |  **|**  | ``if`` ``(`` //PureExp// ``)`` //Stm// ``else`` //Stm// 
  |  |  **|**  | ``{`` //[AnnStm]// ``}`` 
  |  |  **|**  | ``throw`` //PureExp// ``;`` 
  |  |  **|**  | ``try`` //AnnStm// ``catch`` ``{`` //[CatchBranch]// ``}`` //MaybeFinally// 
  |  |  **|**  | //PureExp// ``.`` ``pro_give`` ``(`` //PureExp// ``)`` ``;`` 
  | //AwaitGuard// | -> | //PureExp// 
  |  |  **|**  | ``this`` ``.`` //L// ``?`` 
  |  |  **|**  | //L// ``?`` 
  |  |  **|**  | //AwaitGuard// ``&`` //AwaitGuard// 
  | //Exp// | -> | //PureExp// 
  |  |  **|**  | //EffExp// 
  | //[PureExp]// | -> | **eps**
  |  |  **|**  | //PureExp// 
  |  |  **|**  | //PureExp// ``,`` //[PureExp]// 
  | //PureExp// | -> | //PureExp// ``||`` //PureExp1// 
  |  |  **|**  | //PureExp1// 
  |  |  **|**  | ``let`` ``(`` //Param// ``)`` ``=`` //PureExp// ``in`` //PureExp// 
  |  |  **|**  | ``if`` //PureExp// ``then`` //PureExp// ``else`` //PureExp// 
  |  |  **|**  | ``case`` //PureExp// ``{`` //[CaseBranch]// ``}`` 
  | //PureExp1// | -> | //PureExp1// ``&&`` //PureExp2// 
  |  |  **|**  | //PureExp2// 
  | //PureExp2// | -> | //PureExp2// ``==`` //PureExp3// 
  |  |  **|**  | //PureExp2// ``!=`` //PureExp3// 
  |  |  **|**  | //PureExp3// 
  | //PureExp3// | -> | //PureExp3// ``<`` //PureExp4// 
  |  |  **|**  | //PureExp3// ``<=`` //PureExp4// 
  |  |  **|**  | //PureExp3// ``>`` //PureExp4// 
  |  |  **|**  | //PureExp3// ``>=`` //PureExp4// 
  |  |  **|**  | //PureExp4// 
  | //PureExp4// | -> | //PureExp4// ``+`` //PureExp5// 
  |  |  **|**  | //PureExp4// ``-`` //PureExp5// 
  |  |  **|**  | //PureExp5// 
  | //PureExp5// | -> | //PureExp5// ``*`` //PureExp6// 
  |  |  **|**  | //PureExp5// ``/`` //PureExp6// 
  |  |  **|**  | //PureExp5// ``%`` //PureExp6// 
  |  |  **|**  | //PureExp6// 
  | //PureExp6// | -> | ``~`` //PureExp6// 
  |  |  **|**  | ``-`` //PureExp6// 
  |  |  **|**  | //PureExp7// 
  | //PureExp7// | -> | //QL// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | //QL// ``[`` //[PureExp]// ``]`` 
  |  |  **|**  | //L// 
  |  |  **|**  | ``this`` ``.`` //L// 
  |  |  **|**  | //QU// 
  |  |  **|**  | //QU// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | //Literal// 
  |  |  **|**  | ``(`` //PureExp// ``)`` 
  | //CaseBranch// | -> | //Pattern// ``=>`` //PureExp// 
  | //[CaseBranch]// | -> | //CaseBranch// ``;`` 
  |  |  **|**  | //CaseBranch// ``;`` //[CaseBranch]// 
  | //[Pattern]// | -> | **eps**
  |  |  **|**  | //Pattern// 
  |  |  **|**  | //Pattern// ``,`` //[Pattern]// 
  | //Pattern// | -> | //Literal// 
  |  |  **|**  | //L// 
  |  |  **|**  | //QU// 
  |  |  **|**  | //QU// ``(`` //[Pattern]// ``)`` 
  |  |  **|**  | ``_`` 
  | //EffExp// | -> | ``new`` //QU// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | ``new`` ``local`` //QU// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | //PureExp// ``.`` //L// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | ``this`` ``.`` //L// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | //PureExp// ``!`` //L// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | ``await`` //PureExp// ``!`` //L// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | ``this`` ``!`` //L// ``(`` //[PureExp]// ``)`` 
  |  |  **|**  | //PureExp// ``.`` ``get`` 
  |  |  **|**  | ``pro_new`` 
  |  |  **|**  | //PureExp// ``.`` ``pro_try`` 
  | //Ann// | -> | ``[`` //Ann_// ``]`` 
  | //[Ann]// | -> | **eps**
  |  |  **|**  | //Ann// //[Ann]// 
  | //Ann_// | -> | //PureExp// 
  |  |  **|**  | //T// ``:`` //PureExp// 
  | //AnnStm// | -> | //[Ann]// //Stm// 
  | //[AnnStm]// | -> | **eps**
  |  |  **|**  | //AnnStm// //[AnnStm]// 
  | //AnnDecl// | -> | //[Ann]// //Decl// 
  | //[AnnDecl]// | -> | **eps**
  |  |  **|**  | //AnnDecl// //[AnnDecl]// 
  | //CatchBranch// | -> | //Pattern// ``=>`` //AnnStm// 
  | //[CatchBranch]// | -> | **eps**
  |  |  **|**  | //CatchBranch// //[CatchBranch]// 
  | //MaybeFinally// | -> | ``finally`` //AnnStm// 
  |  |  **|**  | **eps**
  | //MaybeBlock// | -> | ``{`` //[AnnStm]// ``}`` 
  |  |  **|**  | **eps**


